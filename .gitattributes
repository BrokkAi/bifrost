# Policy sources and formatter golds are compared with exact byte ranges.
*.rqlp text eol=lf

# This reviewed upstream artifact is verified by its exact SHA-256 digest.
tests/fixtures/sarif/sarif-schema-2.1.0.json text eol=lf
